This project is the server side of what should be a bigger project the implements a full communication between Android device and PC
that are on the same Wi-Fi network.
The app isn't done and in it current state it supports accepting connection from client and listening to client.
The server is warrped with super simple GUI that makes things a bit easier.