This project is the client side of what should be a bigger project the implements a full communication between Android device and PC
that are on the same Wi-Fi network.
The app isn't done and in its current state it supports connecting to a computer by specifying IP address and port.
after connection been established, the app sending packet to computer until the stop btn is pressed.